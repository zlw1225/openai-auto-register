# 今日工作备忘（2026-03-07）

## 今日目标
打通链路：`openai-auto-register 注册账号 → token 直接喂给 CPA → OpenCode 通过 CPA 使用`

## 今日完成

1. 跑通 `openai-auto-register` 项目并完成基础配置。
2. 修复注册流程超时：将关键请求超时从 `15s` 提升到 `45s`（`register.py`）。
3. 定位关键路径：
   - OpenCode auth：`C:\Users\z2279\.local\share\opencode\auth.json`
   - CPA 配置：`E:\cliproxyapi\config.yaml`
   - CPA token目录：`C:\Users\z2279\.cli-proxy-api\`
4. 改造 `register.py` 与 `run.py`：注册后的 token 不再落在本项目 `tokens/`，而是直接保存到 CPA 目录，命名为 `codex-<email>.json`。
5. 修复 CPA `api-keys` 配置：已写入可用 key `sk-tXzlCFVVwrCNsmST5`（替换默认占位符）。
6. 验证链路成功：
   - `GET http://127.0.0.1:8317/v1/models` 返回 `200`
   - `POST http://127.0.0.1:8317/v1/chat/completions` 返回 `200`

## 当前状态

- 账号池：约 8 个 `codex-*.json`
- CPA 端口：`8317`
- OpenCode 环境变量：
  - `OPENAI_API_BASE=http://127.0.0.1:8317/v1`
  - `OPENAI_API_KEY=sk-tXzlCFVVwrCNsmST5`

## 之后怎么用（最简）

是的，核心就是在本项目目录执行：

```bash
python run.py --count 10
```

但要保证以下前置条件一直成立：

1. CPA 正在运行（监听 `127.0.0.1:8317`）。
2. `E:\cliproxyapi\config.yaml` 的 `api-keys` 包含你在 OpenCode 里使用的 key（当前是 `sk-tXzlCFVVwrCNsmST5`）。
3. OpenCode 继续使用上面的 `OPENAI_API_BASE` / `OPENAI_API_KEY`。

满足以上条件后，`run.py` 生成的新账号会自动进 CPA 池子，OpenCode 无需额外手工导入。
